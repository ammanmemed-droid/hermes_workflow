# secrets — never commit
auth.json
*.auth.json
.env
.env.*
*.env
*.lock

# hermes runtime state (not workflow assets)
state.db*
sessions/
logs/
cache/
*.log
skills/.usage.json
skills/.hub/

# codex runtime built-in skills (regenerated by codex itself)
codex/skills/.system/

# OS junk
.DS_Store
Thumbs.db

# keep kanban + memories + skills
!memories/MEMORY.md
